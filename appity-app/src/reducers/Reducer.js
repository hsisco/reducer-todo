export const appReducer = (state, action) => {
  switch(action.type) {
  
  }
}

export const initialState = { list: [
  {
    item: 'Learn about reducers',
    completed: false,
    id: Date.now(),
  }
]}